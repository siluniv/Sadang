{{!< ../default}}

{{#contentFor 'title'}}{{t 'Log in to {siteTitle}' siteTitle=@site.brand_site_title}}{{/contentFor}}

<div class='o-wrapper'>
  {{#if @member}}
    {{!-- Logged in: Redirect home --}}
    <script>window.location = '{{@site.url}}';</script>
  {{else}}
    {{!-- Not logged in: Signin form --}}
    {{bluedot_signin}}
  {{/if}}
</div>